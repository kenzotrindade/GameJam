import { Player } from "../entities/player"

const stateSelect = {
    null : 0,
    p1 : 1,
    p2 : 2,

}
const selectPlayer = {
    id : 'red',
    skin : 'RedProtector',
    Image : img.RedProtector,
    state : stateSelect.null,
}

if (stateSelect === stateSelect.null) {
    
}